function add(a, b) {
  const total = a + b;
  if (b > 5) {
    const sum = 15 + a + b;
  } else {
    const sum = 12 + a + b;
    var current = sum;
  }
  console.log(current);
  return total;
}
add(3, 5);
